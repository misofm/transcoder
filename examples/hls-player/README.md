# Local HLS player

Set `MISO_HLS_ARTIFACT` to a verified artifact directory, then run `bun run player`. The read-only development server exposes only the flat HLS files in that directory on localhost. It performs no remote loading or publication.
